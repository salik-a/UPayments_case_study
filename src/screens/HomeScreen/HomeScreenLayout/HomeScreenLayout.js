import { View, Text, SafeAreaView, Button } from 'react-native'
import React from 'react'

import styles from './HomeScreenLayout.style'

export default function HomeScreenLayout({ navigation }) {
    return (
        <SafeAreaView>
            <Text>HomeScreenLayout</Text>
        </SafeAreaView>
    )
}